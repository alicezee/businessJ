const Contacts = () => {
    return (
        <div>
            <div>
                <h1 className = "font-playD">Contacts</h1>
            </div>
        </div>

    );
};

export default Contacts;